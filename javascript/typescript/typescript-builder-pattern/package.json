{
  "scripts": {
    "start": "npm run compile && npm run run",
    "compile": "npx tsc --strict --target es2015 index.ts",
    "run": "node index.js"
  },
  "dependencies": {
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  }
}
