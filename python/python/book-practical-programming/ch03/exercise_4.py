def difference(n1: int, n2: int) -> int:
    """Calculates the absolute difference between the two numbers given

    >>> difference(1, 5)
    4
    >>> difference(3, 3)
    0
    """

    return abs(n1 - n2)
